{
  "AiChatSampleSettings": {

    "OllamaEndpointUri": "http://localhost:11434/", // The endpoint URI of the Ollama service

    "ChatModelId": "llama3.1:8b", // The model ID of the AI chat model to use for example "llama3.1"
    "ChatModelSystemPrompt": "You are a friendly and helpful AI assistant.", // The system prompt to use for the AI model

    // Optional AI vision model for image analysis
    "VisionModelId": "llava:13b", // The model ID of the AI vision model to use for example "llava"
    "VisionModelSystemPrompt": "You are a AI assistant who describes images in detail.",

    // Optional AI embeddings model for RAG
    "EmbeddingsModelId": "all-minilm", // The model ID of the model for example "all-minilm" to use for embeddings
    "EmbeddingsDataFilePath": "SampleData/Data.en.json", // The path to the file containing the data for the embeddings
    "UseEmbeddingsPrompt": "Use the following data as additional context for answering the next question:" // The prompt to instract the AI model to use the embeddings

  }
}
